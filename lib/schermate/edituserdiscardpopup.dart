/*import 'package:flutter/material.dart';

Future<bool> editUserdiscardpopup(context) async {
  return await showDialog(
      context: context,
      builder: (context) {
        return AlertDialog(
          title: Text('Annullare le modifiche? ogni cambiamento verrà perso'),
          actions: [
            TextButton(
              onPressed: () {
                Navigator.of(context).pop(true);
              },
              child: Text('Conferma'),
            ),
            TextButton(
              onPressed: () {
                Navigator.of(context).pop(false);
              },
              child: Text('Annulla'),
            ),
          ],
        );
      });
}*/
